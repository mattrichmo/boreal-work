# rust-cli-build

Command: `cargo build --locked -p boreal-cli`

Scope: CLI build, not release packaging

Result: **blocked_tool_unavailable**; exit 127.

```text
[Errno 2] No such file or directory: 'cargo'

```
