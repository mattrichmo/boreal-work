# rust-domain

Command: `cargo test --locked -p boreal-domain --test m02_status`

Scope: authored M02 evaluator tests

Result: **blocked_tool_unavailable**; exit 127.

```text
[Errno 2] No such file or directory: 'cargo'

```
