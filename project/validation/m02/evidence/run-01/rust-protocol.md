# rust-protocol

Command: `cargo test --locked -p boreal-protocol --test m02_status_wire`

Scope: authored additive DTO tests

Result: **blocked_tool_unavailable**; exit 127.

```text
[Errno 2] No such file or directory: 'cargo'

```
