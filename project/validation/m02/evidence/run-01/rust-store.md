# rust-store

Command: `cargo test --locked -p boreal-store --test m02_claim`

Scope: authored canonical claim/race tests

Result: **blocked_tool_unavailable**; exit 127.

```text
[Errno 2] No such file or directory: 'cargo'

```
