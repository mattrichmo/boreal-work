# Retained execution: installer-recovery-check

Command: `node scripts/build-installer.mjs --check`

Exit: 0. Recovered body/bundle agree byte-for-byte with embedded install.sh payload.

```text
Installer bundles match their source.

```
