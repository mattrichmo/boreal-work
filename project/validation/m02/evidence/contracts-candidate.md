# Retained execution: contracts-candidate

Command: `python3 project/spec/validate_contracts.py`

Exit: 1. Rejected candidate: acceptance fixture_version disagreed with protocol manifest.

```text
FAIL: acceptance-profiles.json: fixture revision differs from manifest

```
