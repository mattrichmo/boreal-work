# PF-S02-T11 — attempt 34 start

This bounded lane addresses the CLI regressions exposed by the PF-S03 action/status bridge. Scope is limited to `crates/cli/src/main.rs` and `crates/cli/src/service.rs`; no plan, state, ledger, store-core, or runtime-memory files are in scope.

The source base is `HEAD 3017a1dbebaa7945f82b2a2512ec0c1eabbd69c9` with an intentionally dirty working tree. This lane does not commit or push.
