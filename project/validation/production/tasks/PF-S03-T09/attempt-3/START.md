# PF-S03-T09 — attempt 3 start

This bounded lane repairs CLI behavior exposed by the PF-S03 status/action integration. It owns only the CLI adapter and service serialization paths. It must preserve server-derived action descriptors, fail-closed mutation authorization, and the current TUI contract.

Source base: `HEAD 3017a1dbebaa7945f82b2a2512ec0c1eabbd69c9`; working tree intentionally dirty. No plan/state/ledger/memory edits, commit, or push are allowed.
