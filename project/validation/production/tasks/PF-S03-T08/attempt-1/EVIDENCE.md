# PF-S03-T08 — attempt 1 evidence

## Evidence record

- Record: `PF-S03-T08/attempt-1`.
- Evidence class: pure-domain and deterministic/exhaustive test evidence.
- Input source: `784a41b3802c29a76721c55eef2e9493283396c2`, dirty combined tree.
- Contract identities read:
  - `project/spec/production/contract-manifest.json` SHA-256
    `131a0f2028629dab2ac372159cd19d67d33326e23085a8f1956fcbed3a155aa`.
  - `project/spec/production/status-and-actions.md` SHA-256
    `b2b41ffd640811118e2c8f0ac0ba9c60d79cc46ccc73135cdcf609ae384b3a94`.
  - `project/spec/transition-table.md` SHA-256
    `4a22bceb49b8d40d96a872f2ae3aed8b79a81d5636339c609914a05b3a2a9d38`.
- Final owned source SHA-256:
  - `crates/domain/tests/production_properties.rs`:
    `19c93cdd9b6fbb708fbed54977aa04b0ce74f6128965af902acdad1ae384dcf6`.
  - `project/validation/production/domain/PF-S03-T08-ORACLE.md`:
    `8cb4f719b4dd983f2b74f02e0305c9f2eeabcb4be28ba7057fe59d224b70daff`.
- Host/toolchain: macOS workspace, Rust/Cargo `1.85.0`.

## Covered invariants

1. Four deterministic LCG seeds × 256 cases compare repeated status evaluation
   and reordered equivalent facts. Reasons retain the selected primary first,
   sort only the remaining unique reasons, and canonicalize gate gaps and
   affected dependents.
2. Failed/released/cancelled historical attempts do not alter current status;
   closed/cancelled terminal status remains stable until the explicit reopen
   lifecycle transition.
3. Lease and hard-budget exact equality is expired, the earlier clock wins, a
   hard deadline is not renewed by heartbeat, and status becomes
   `expired_review` at the exact boundary.
4. Every `AttemptPhase × AttemptOperation` pair is compared with an explicit
   legal-transition oracle. Illegal operations must not mutate the attempt.
5. Every `PersistedLifecycle × WorkOperation` pair is compared with an explicit
   legal-transition oracle. Derived statuses are rejected as writes.
6. Only `closed` satisfies the default dependency rule; valid paths, cycles,
   and cross-project dependency edges are distinguished.
7. Actor/action decisions are total over the full public action vocabulary,
   deterministic, partition every action exactly once, and round-trip allowed
   descriptors through their request form.
8. Foreign receipt subjects, stale fences, missing independent review, and
   self-review fail closed without fabricating acceptance.

## Observed result

The final focused target passed 8/8. Full `boreal-domain` tests passed, strict
domain clippy passed, formatting passed, and `git diff --check` passed. No
store, application, service, TUI, native-package, verifier, or release claim
is made by this evidence.

## Preserved limitations

- The generated cases exercise the current public pure-domain inputs. They do
  not prove that store snapshots retain all immutable requirements, that every
  service mutation reruns the evaluator transactionally, or that persisted
  receipts were generated by a genuine verifier.
- The source tree is dirty and therefore is not a release identity.
- The initial missing-test-target result and intermediate failing test attempts
  are retained in the worker narrative and were not overwritten as passes.

