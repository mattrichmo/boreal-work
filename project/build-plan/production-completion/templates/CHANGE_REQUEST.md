# Bounded plan / write-boundary change request

Request ID / source task / plan version:
Why existing scope is insufficient:
New invariant/finding and source evidence:
Requested task ID(s), prerequisites and successor gates:
Exact proposed worker paths / shared integration paths:
Contract/schema/protocol/migration/release impact:
Conflict check and independent reviewer:
Required acceptance cases and evidence layer:
Existing attempts/history preserved:
Scope/authority approval:

Do not edit outside the current grant until the coordinator accepts the request and updates the graph/locks. Remediation children use `kind: remediation` and unused task IDs below T90. They may depend on accepted original work/T90, not T91/T92; T91 gains them as prerequisites and T90 is not retroactively gated on its own fixes. Failed revalidation is an evidence reference, not an accepted prerequisite. Validate acyclicity. A change in product scope requires explicit owner decision and updated dependent cards.
