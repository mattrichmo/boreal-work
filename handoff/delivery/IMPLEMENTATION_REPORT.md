# Boreal v2 implementation handoff — 2026-09-25

## Delivery state

The active 8-sprint queue is complete: **48/48 task implementation and handoff items are checked, with 0 open and 8/8 sprint areas integrated in the combined source tree.** The work implements canonical persistence and status/actions, project authority and isolation, lifecycle/evidence, cycle and container rollups, CLI/TUI workflows, source and memory reconciliation, migration/maintenance, and release packaging. The combined tree compiled; TUI typechecking and the installed-package smoke check passed. Tests did not block task completion.

The TUI test command could not bind its temporary Unix-domain socket in the sandbox (`listen EPERM`); this is an environment limitation, not a failed assertion. The package smoke check passed and covered release manifest/assets, installed binary behavior, local initialization, repeated project identity readback, doctor, two-project isolation, and rejection of uninitialized/foreign-project access. Existing compiler warnings remain. No release was published; the package was built and installed only in a disposable temporary prefix.

## Exact source identity

- Workspace: `/Users/cybertron/Code/boreal-work`
- Branch: `codex/apply-responsive-terminal-overlay`
- Current HEAD: `abf87bb528b55632499bb246c10aeb902680a582` (the worktree was dirty; this commit does not identify the uncommitted changes)
- Changed paths in this commit: **155**; see [CHANGED_FILES.tsv](CHANGED_FILES.tsv)
- Changed source overlay: `boreal-v2-changed-source-overlay.zip` (143 files; SHA-256 `81e2c56470f8b6c6bde34d8cda342b159ebc46c9d22027f9a4fa62f7045bd484`)
- Broad text source snapshot: `boreal-v2-reference-20260925T151846780Z.zip` (1816 files; SHA-256 `ba6aaee4d67c1376e474d1a003637d01d977dca8443b784469080c0eb3aebbcb`)
- The changed-source overlay excludes delivery artifacts, project validation task runtime files, generated TUI compile cache, build output, dependencies, binary files, and ZIP archives.
- The broad snapshot was generated by `create-zips.mjs`; it excludes generated output, dependencies, runtime state, binary assets, and handoff/delivery files.

## Commands and outcomes

- `cargo check --workspace` — passed.
- `npm --prefix apps/tui run typecheck` — passed.
- `cargo test -p boreal-cli --bin bwrk service::tests::operation_readback_includes_the_durable_receipt_payload -- --exact` — passed (1 test).
- `bash scripts/release/package-smoke.sh` — passed; built and installed the package in a temporary prefix and verified package and project-isolation behavior.
- `npm --prefix apps/tui test` — could not run to completion because the sandbox denied binding its temporary Unix-domain socket (`EPERM`).
- `git diff --check` — passed before commit preparation; staged changes were checked again.

## Merge instructions

1. Treat this directory as one dirty shared worktree overlay based on `abf87bb528b55632499bb246c10aeb902680a582`; do not reset, clean, or replace it with either ZIP.
2. Review `CHANGED_FILES.tsv` against the live worktree and preserve unrelated pre-existing modifications and untracked files. The inventory describes current Git changes, not authorship.
3. The 48 items are complete. No implementation work remains in this queue; broader product acceptance is tracked separately in the historical plan and is not a blocker for this queue.
4. Create commits/PRs or publish a release only through the normal repository process; this handoff does not authorize publication.
